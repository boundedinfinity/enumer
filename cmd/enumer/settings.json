{
    "yaml.schemas": {
        "enum.schema.json": [
            "**/*.enum.yaml"
        ]
    },
    "json.schemas": [
        {
            "fileMatch": ["**/*.enum.json"],
            "url": "enum.schema.json"
        }
    ]
}
